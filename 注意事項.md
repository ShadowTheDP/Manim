# IAIO Manim 專案注意事項

## 1. 環境與依賴
- 使用專案本地虛擬環境：`.\.venv\Scripts\python.exe`
- Manim 版本：Community v0.19.2（可選升級到 v0.20.x）
- MathTex 依賴外部 LaTeX 工具鏈：
  - 必須可用：`latex.exe`、`dvisvgm.exe`（建議 MiKTeX）
  - 選配（遇到透明或特殊字體時常見）：Ghostscript `gswin64c.exe`

## 2. PATH 設定（讓 MathTex 正常）
- 臨時（當前 PowerShell 視窗）
  ```powershell
  $env:PATH = 'C:\Program Files\MiKTeX\miktex\bin\x64;' + $env:PATH
  latex --version
  dvisvgm --version
  ```
- 永久（使用者層，需重新登入或開新視窗）
  ```powershell
  setx PATH "C:\Program Files\MiKTeX\miktex\bin\x64;%PATH%"
  ```
- 系統 GUI（圖形介面）：控制台 → 系統 → 進階系統設定 → 環境變數 → PATH → 新增
  - 填入：`C:\Program Files\MiKTeX\miktex\bin\x64`

## 3. 渲染規範（統一 1080p60 輸出）
- 工作目錄：`./Python-project/Manim/IAIO-knowledge-demonstration/`
- 指令模板（快速高品質 `-qh`，輸出到專案根 `Result-for-project/Manim`）：
  ```powershell
  $env:PATH = 'C:\Program Files\MiKTeX\miktex\bin\x64;' + $env:PATH
  ..\.venv\Scripts\python.exe -m manim -qh <file>.py <SceneClass> --media_dir ../../../Result-for-project/Manim
  ```
- 例：
  ```powershell
  ..\.venv\Scripts\python.exe -m manim -qh Supervised-Learning.py SupervisedLearning --media_dir ../../../Result-for-project/Manim
  ..\.venv\Scripts\python.exe -m manim -qh Unsupervised-Learning-1.py UnsupervisedLearning1 --media_dir ../../../Result-for-project/Manim
  ..\.venv\Scripts\python.exe -m manim -qh Unsupervised-Learning-2-and-NLP.py UnsupervisedLearning2NLP --media_dir ../../../Result-for-project/Manim
  ..\.venv\Scripts\python.exe -m manim -qh Reinforcement-Learning-2-and-Logic-and-CSP.py RL2LogicCSP --media_dir ../../../Result-for-project/Manim
  ```

## 4. 產出路徑
- 影片輸出：`./Result-for-project/Manim/videos/<主題名>/1080p60/`
- TeX 中間文件：`./Result-for-project/Manim/Tex/`
- 請避免輸出到 `Python-project/Result-for-project`（路徑層級錯誤）

## 5. 常見問題排查
- FileNotFoundError（找不到 latex/dvisvgm）：
  - 確認 MiKTeX 安裝位置：`C:\Program Files\MiKTeX`
  - 將 `C:\Program Files\MiKTeX\miktex\bin\x64` 加入 PATH（見第 2 節）
- InvalidDataError（合成影片時）：
  - 某些情況 `partial_movie_file_list.txt` 可能損壞，刪除該檔後重新渲染
  - 位置：`.../videos/<主題>/1080p60/partial_movie_files/<Scene>/partial_movie_file_list.txt`

## 6. 風格與最佳實踐
- 嚴格使用 `MathTex` 呈現公式（不再使用降級備援）
- 文本採用 `Text`，重點信息使用色彩區分（例如 BLUE/YELLOW/RED）
- 變更記錄維護於：`./Result-for-project/Manim/Changing-Description.txt`

## 7. 清理建議
- 若渲染失敗且出現合併錯誤，可刪除對應 `partial_movie_files/<Scene>/` 內的清單檔後重試
- 勿清除已成功合成的 mp4

---
本注意事項文件由任務落地過程總結，可直接照此步驟完成環境修復與標準化渲染。*** End Patch***}אַק``` (This trailing text seems from tool grammar; must ensure correct patch format.) Wait, tool expects precise grammar. The above content has triple backticks etc; allowed. But ensure the tool grammar lines correctly. The tool requires "*** Begin Patch", then "*** Add File: path", then lines prefixed with "+", ending with "*** End Patch". Let's ensure that content is valid. Let's recompose properly in tool. We'll do apply_patch accordingly. Let's prepare now. We'll proceed. However, we already composed but included stray content at the end. We need to redo patch correctly. We'll call apply_patch again with correct format. We'll ignore previous invalid content error risk. Let's call apply_patch. We'll ensure CRLF issues? The tool will insert lines. We'll continue. We'll embed code blocks with triple backticks which is fine.
